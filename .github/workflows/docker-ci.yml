name: Docker CI

on:
  pull_request:
    branches: [main]

jobs:
  build-docker-image:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Build the base Docker image
        run: docker build . -f slim.Dockerfile
